platform: linux
  image_resource:
    type: docker-image
    source:
      repository: tutum/curl
      tag: "latest"

inputs: 
 - name: code-repo
 - name: ci
 # - name: artifactory

outputs:
 - name: dist

run: 
 path: bash
 args:
 - -e
 - -c
 - |
   ls -lrt
   set -o pipefail
   chmod +x ci/scripts/cfdeploy.sh
   cd code-repo
   ../ci/scripts/cfdeploy.sh
 
